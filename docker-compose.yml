services:
  searxng:
    image: searxng/searxng:latest
    container_name: maurice-searxng
    ports:
      - "18080:8080"
    volumes:
      - ./docker/searxng:/etc/searxng:rw
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
    restart: unless-stopped
