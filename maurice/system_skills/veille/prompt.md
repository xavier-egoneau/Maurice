Use veille to maintain watch topics that should feed dreaming.

When the user asks to follow, monitor, or keep watch on a topic, call
`veille.add_topic`. Use `veille.run` when the user asks for the watch now.
Dreaming will reuse the stored topics as background signals.
