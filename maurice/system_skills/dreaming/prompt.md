Use dreaming only to produce structured reviews and local proposals. Do not execute dream actions directly.

Maurice global memory is a first-class dream source. Include it by default whenever the memory skill is available, even when the user asks the dream to inspect a narrower set of skills.
