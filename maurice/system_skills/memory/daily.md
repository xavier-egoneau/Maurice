Memory can contribute durable context for the current agent.

Surface only memories that are useful for today's decisions: active project
facts, user preferences, repeated blockers, and commitments that should not be
lost. Do not read another agent's memory unless another loaded skill explicitly
provides that cross-agent context.
