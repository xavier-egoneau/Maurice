Daily owns the morning digest contract.

Use the latest agent-scoped dream report as the main source, then consult the
`daily.md` fragments from loaded skills to decide what belongs in the digest.
A useful daily should surface active projects, proposed actions, useful ideas,
and cross-project links. It should not create work by itself; actions remain
proposals until a project skill or the user validates them.
